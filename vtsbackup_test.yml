models:
  snapshot:
    description: "test backup with snapshot"
    storages:
      minio:
        type: minio
        bucket: vtsbackup-test
        endpoint: http://127.0.0.1:9000
        path: backups
        access_key_id: test-user
        secret_access_key: test-user-secret
